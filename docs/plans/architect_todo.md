# Architect Todo

## In Progress

## Backlog

### [ ] Decide where to embed the G3X system interconnect diagram in Section 17 (2026-04-18)

The SVG (`docs/references/diagrams/g3x-system-architecture.svg`) is finished and ready to publish to the WR as `assets/diagrams/sec17-g3x-system-interconnect.svg`. The Writer needs a home page for it.

Section 17 has no system overview page — the TOC goes straight to component pages. Options:

1. Add a new `avionics-g3x-system` system overview page (Type 1a) to the TOC and embed the diagram there
2. Embed it on an existing component page (the GDU 460 PFD is the central display and a candidate)

Tom wants your opinion before the Writer publishes. Update `docs/plans/writer_todo.md` with the decision so the Writer can proceed.
