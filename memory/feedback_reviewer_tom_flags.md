---
name: Reviewer: @@TOM flags block approval
description: If Tom says "approved" but open @@TOM flags remain, call his attention to the unresolved information and suggest marking pending instead.
type: feedback
---

When Tom says a page is "approved," check for open @@TOM flags before updating the review log. If flags remain, call his attention to the specific unresolved information and suggest marking the page pending instead.

**Why:** Tom confirmed this explicitly on 2026-04-18. @@TOM flags represent unverified facts or decisions — approving a page with open flags would let unverified content into the manual.

**How to apply:** Any time Tom gives approval, scan the page for `@@TOM:` before writing "Approved" to the review log. If found, say: "There's still an open @@TOM flag on [topic] — suggest marking pending until that's resolved."
